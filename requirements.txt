pandas
pyarrow
matplotlib
zstandard
requests
jupyter
wordfreq
huggingface_hub
fsspec
vaderSentiment
joblib
streamlit
python-dotenv
pip-system-certs

# blpapi (Bloomberg, WORK LAPTOP only, for pull_bloomberg_prices.py) is NOT on
# PyPI, so it is intentionally left out of this file. Install it separately from
# Bloomberg's own index, with the Terminal running:
#   python -m pip install --index-url=https://blpapi.bloomberg.com/repository/releases/python/simple/ blpapi --user
