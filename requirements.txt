pandas
pyarrow
numpy
matplotlib
scipy
zstandard
requests
jupyter
nbconvert
ipykernel
wordfreq
huggingface_hub
fsspec
vaderSentiment
finvader
joblib
pytest
pip-system-certs
streamlit
plotly

# blpapi (Bloomberg, for pull_bloomberg_prices.py - install on each machine)
# is not on PyPI. Install it separately from Bloomberg's own index, with the
# Terminal running:
#   python -m pip install --index-url=https://blpapi.bloomberg.com/repository/releases/python/simple/ blpapi --user
