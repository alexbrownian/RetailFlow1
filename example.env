# Rename this file to .env and fill in the values. .env is git-ignored -
# API keys NEVER go in code or notebooks. See data_ingestion/LIVE_INGESTION.md
# for where each credential comes from and the rate limits that apply.

# --- Reddit (PRAW "script" app: https://old.reddit.com/prefs/apps/) ---
REDDIT_APP_NAME =
REDDIT_PERSONAL_USE =
REDDIT_SECRET =
REDDIT_USERNAME =
REDDIT_PASSWORD =

# --- StockTwits: no key needed for read-only symbol streams ---

# --- X / Twitter (OFFICIAL v2 API - paid) ---
# Leave empty until you pay for API access. The moment a bearer token is
# here, fetch_x_live.py starts working - no other changes needed.
# Get it: developer.x.com -> Projects & Apps -> your app -> Bearer Token.
X_BEARER_TOKEN =


# checl check_live_ingestion to verify if it works or not